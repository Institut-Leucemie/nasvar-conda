{% set version = "1.1.0.post13" %}

package:
  name: nasvar
  version: {{ version }}

source:
  git_url: https://github.com/jwanglab/nasvar.git
  git_rev: e1e31a2fbc752f6d85a8f86283e20b575066a6f1

build:
  number: 0

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - cargo-bundle-licenses

test:
  commands:
    - nasvar --version
    - aggregate --help

about:
  home: https://github.com/jwanglab/nasvar
  summary: "Variant caller for tumor-only long-read (Oxford Nanopore) sequencing"
  license: LicenseRef-UNC-NonCommercial
  license_file:
    - LICENSE
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - drosofff
